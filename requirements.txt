# Core dependencies for Print Shop AI Order Guardrail PoC

# Image/PDF processing (make PyMuPDF optional to avoid crashes)
Pillow>=10.0.0
PyMuPDF>=1.23.0

# Data handling
python-dateutil>=2.8.0

# Note: PyMuPDF (fitz) is used for PDF processing but may fail in serverless
# If it fails, resolution checking for PDFs will be unavailable
# Pillow is required for image processing
